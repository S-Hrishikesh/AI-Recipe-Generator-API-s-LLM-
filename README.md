# AI-Recipe-Generator-API-s-LLM-

Vision:
Create a Projct that concurrently utilizes 2 API's( 2nd API is reliant on the output of 1st API) and these get the response from the pre-trained LLM's(Groq,(initially used gemini flash but encountered server error several times))

Current Update:
I have successfully included the Groq API into the project using the POST API call method.

Futute Updates:
include a diffusion model to generate an image of the final recipe based on the recipe given by the Groq LLM.
